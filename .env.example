# ---- Local development (`pnpm dev`) ----
# apps/api/.env overrides these per-package; this file documents the shape.
DATABASE_URL=postgresql://lcm:lcm@localhost:5432/lcm
PORT=8080
LOG_LEVEL=info
NODE_ENV=development

# ---- Production docker compose ----
# Copy this file to `.env` and adjust before `docker compose up`.
# COMPOSE_FILE points the default `docker compose ...` lookup at the file in
# docker/, so you don't need `-f docker/docker-compose.yml` on every command.
COMPOSE_FILE=docker/docker-compose.yml

# Postgres credentials baked into both the db and api containers.
POSTGRES_USER=lcm
POSTGRES_PASSWORD=change-me-before-deploying
POSTGRES_DB=lcm

# Host port the web container exposes (maps to nginx 80 inside the container).
HTTP_PORT=80

# Set to `true` for the first boot so the api seeds reference data; flip back
# to false on subsequent restarts (seeding is idempotent but slow at startup).
SEED_ON_BOOT=false
